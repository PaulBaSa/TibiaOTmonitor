# Server Configuration
# Node listens on this port (plain HTTP, internal only).
# SSL/HTTPS is handled by nginx — see nginx/tibia-monitor.conf
PORT=3000

# CORS - comma-separated list of allowed origins (use * to allow all)
ALLOWED_ORIGINS=*

# Session expiry in minutes (inactive SSH sessions will be closed)
SESSION_EXPIRY_MINUTES=30
